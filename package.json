{
  "name": "conversational-booking",
  "description": "With words!",
  "version": "2.0.5",
  "skill": {
    "namespace": "conversational-booking",
    "upgradeIgnoreList": []
  },
  "homepage": "https://github.com/spruce-conversational-booking-skill",
  "bugs": {
    "url": "https://github.com/spruce-conversational-booking-skill/issues"
  },
  "keywords": [],
  "scripts": {
    "boot": "node build/index",
    "boot.local": "node -r ts-node/register -r tsconfig-paths/register ./src/index",
    "health": "yarn boot --health",
    "health.local": "yarn boot.local --health",
    "build.ci": "yarn build.tsc && yarn build.resolve-paths && yarn lint",
    "build.dev": "yarn build.tsc --sourceMap ; yarn resolve-paths.lint",
    "build.copy-files": "mkdir -p build && rsync -avzq --exclude='*.ts' ./src/ ./build/",
    "build.dist": "yarn build.tsc && yarn build.resolve-paths",
    "build.resolve-paths": "resolve-path-aliases --target build --patterns '**/*.js,**/*.d.ts'",
    "build.tsc": "yarn build.copy-files && tsc",
    "clean": "yarn clean.build",
    "clean.all": "yarn clean.dependencies && yarn clean.build",
    "clean.build": "rm -rf build/",
    "clean.dependencies": "rm -rf node_modules/ package-lock.json yarn.lock",
    "fix.lint": "eslint --fix --cache '**/*.ts'",
    "heroku-postbuild": "yarn build.dev",
    "lint": "eslint --cache '**/*.ts'",
    "lint.tsc": "tsc -p . --noEmit",
    "post.watch.build": "yarn build.copy-files && yarn build.resolve-paths",
    "rebuild": "yarn clean.all && yarn && yarn build.dev",
    "release": "semantic-release",
    "resolve-paths.lint": "yarn build.resolve-paths ; yarn lint",
    "test": "jest",
    "update.dependencies": "yarn clean.dependencies && yarn",
    "watch.build.dev": "tsc-watch --sourceMap --onCompilationComplete 'yarn post.watch.build'",
    "watch.lint": "concurrently 'yarn lint' \"chokidar 'src/**/*' -c 'yarn lint.tsc'\"",
    "watch.rebuild": "yarn clean.all && yarn && yarn watch.build.dev",
    "watch.tsc": "tsc -w"
  },
  "dependencies": {
    "@sprucelabs/calendar-utils": "^35.0.9",
    "@sprucelabs/error": "^5.0.598",
    "@sprucelabs/globby": "^0.0.7",
    "@sprucelabs/mercury-client": "^39.0.12",
    "@sprucelabs/mercury-core-events": "^20.0.10",
    "@sprucelabs/mercury-types": "^44.0.11",
    "@sprucelabs/schema": "^28.6.5",
    "@sprucelabs/spruce-conversation-plugin": "^57.0.16",
    "@sprucelabs/spruce-core-schemas": "^38.0.11",
    "@sprucelabs/spruce-event-plugin": "^57.0.16",
    "@sprucelabs/spruce-event-utils": "^36.0.11",
    "@sprucelabs/spruce-permission-plugin": "^28.0.9",
    "@sprucelabs/spruce-skill-booter": "^57.0.16",
    "@sprucelabs/spruce-skill-utils": "^28.1.121",
    "dotenv": "^16.0.3",
    "node-nlp": "^4.26.1"
  },
  "devDependencies": {
    "@sprucelabs/jest-json-reporter": "^7.0.22",
    "@sprucelabs/jest-sheets-reporter": "^3.0.3",
    "@sprucelabs/resolve-path-aliases": "^1.1.207",
    "@sprucelabs/semantic-release": "^4.0.8",
    "@sprucelabs/spruce-test-fixtures": "^57.0.16",
    "@sprucelabs/test": "^7.7.437",
    "@sprucelabs/test-utils": "^3.4.34",
    "@types/node": "^18.15.11",
    "chokidar-cli": "^3.0.0",
    "concurrently": "^8.0.1",
    "eslint": "^8.37.0",
    "eslint-config-spruce": "^10.12.0",
    "jest": "^29.5.0",
    "jest-circus": "^29.5.0",
    "prettier": "^2.8.7",
    "ts-node": "^10.9.1",
    "tsc-watch": "^6.0.0",
    "tsconfig-paths": "^4.2.0",
    "typescript": "^5.0.3"
  },
  "engines": {
    "yarn": "1.x"
  },
  "jest": {
    "testRunner": "jest-circus/runner",
    "maxWorkers": 4,
    "testTimeout": 120000,
    "testEnvironment": "node",
    "testPathIgnorePatterns": [
      "<rootDir>/tmp/",
      "<rootDir>/src/",
      "<rootDir>/node_modules/",
      "<rootDir>/build/__tests__/testDirsAndFiles/"
    ],
    "testMatch": [
      "**/__tests__/**/*.test.js?(x)"
    ],
    "moduleNameMapper": {
      "^#spruce/(.*)$": "<rootDir>/build/.spruce/$1"
    }
  }
}
